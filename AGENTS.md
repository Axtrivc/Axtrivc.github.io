# Axtrivc's Blog — Hexo 项目说明

Hexo 7 + Butterfly 主题的个人博客,部署到 GitHub Pages(Axtrivc.github.io)。

## 用户偏好(持久指令)

- **改完代码后自动 commit 并 push,无需逐次确认。**(用户 2026-07-22 明确授权)
  - commit message 用中文、遵循仓库现有 conventional commits 风格(如 `fix(football): ...` / `chore: ...`)
  - 仅限常规 commit/push;force-push、rebase、删分支等破坏性操作仍需先问
- **git 代理走 `http://127.0.0.1:7897`(全局 .gitconfig 已配置,用户 2026-08-27 指定"以后都是这样")。** push 报 "Failed to connect" 类错误先确认代理进程在跑;不要用 `git -c http.proxy=` 绕过(仅当 7897 确实不可用且需紧急推送时临时绕过,事后说明)

## 构建与部署

- `npx hexo generate` 构建到 `public/`;`npx hexo server` 本地预览
- push 到 `master` 触发 GitHub Actions(`.github/workflows/deploy.yml`)构建并部署到 `main` 分支
- `public/`、`db.json`、`node_modules/` 不进仓库(编译产物,CI 重建)

## 自定义结构

- `scripts/`:注入脚本(**Hexo 自动加载 `scripts/` 目录下所有 js 作为插件**,无需注册;`_config.yml` 里曾经的 `scripts:` 列表是无效配置键,2026-08-07 已删除)。**旧 v1 日报生成器已移至 `_reference/football-daily-v1.js`,勿移回 `scripts/`——放回即会在每次 `hexo generate` 时自动执行,覆盖 v2 日报**
  - `hero-inject.js`:首页 hero(ASCII 河流 shader + 左下角 typed 副标题),**v9(2026-08-07)起 hero → 页面过渡并入动画本体,独立渐变带/桥接层/`__heroFrameHook` 取样逻辑已全部删除,勿复活**——两个独立渲染面的硬接边就是用户肉眼可见分界线的根因(v7 渐变带、v8 桥接交叉淡化均被否)。现在:canvas 向下延伸 `--hero-tail`(clamp(240px,32dvh,420px),`:root` 定义在 `source/hero/index.html`;river-hero.js `size()` 用 `innerHeight*0.32` 同公式加高,`.hero-shell`/`.hero-text`/`.hero-typed-wrap` 的 bottom 都用 `calc(var(--hero-tail) + …)` 让位),尾部在 shader 内由 `smoothstep(1.0, sceneK, uvS.y)` 溶解到 `u_pageBg`(= `--page-bg`,hex/rgb 都解析,监听 window `themechange`;溶解放在 vignette/grain 之后,底部 = 纯页面色,与 main 零色差)。**场景区(100dvh)像素与加高前完全一致**是硬约束:shader 里一切 `u_res.y` 派生量(asp/asp2/云·极光·水点阵 cell/AA `px = 1.0/u_res`)都改用 `u_sceneH`(场景区设备高),`uvS = uv * (1, u_res.y/u_sceneH)` 只在 main() 重映射,天空组件在 uvS.y>1 自动门控归零(地平线以下无天空/星辰/云/极光),峡谷地面与河流点阵在尾部自然延续;sky FBO 与 SKY_SYNC 镜像给 sky pass 的 res.y 也换成 sceneHDev;txCanvas 文字层高 = sceneHDev(boot 文字布局不变)。兜底链:placeholder/still 尾部带 `mask-image` 渐隐,露出 `.hero` 底色渐变(场景深蓝 → `--page-bg`,index.html);`freezeStaticHero()` 的 `toDataURL` 整幅截图自带尾部,无需取样对齐;sceneHDev/pageBg/readPageBg 声明必须先于首次 `sizeText()` 调用(TDZ 坑);`TERRAIN_WORKER_TIMEOUT_MS` 同类二犯(2026-08-15,hero 线上冻死 7 天):IIFE 顶层 const 按**源码顺序**求值——函数声明会提升但 const 不会,常量曾被放在 `buildTerrainTextureAsync` 定义旁(首次调用点之后 ~600 行),调用时处于 TDZ,Promise executor 里抛 ReferenceError **只会 reject、不会同步冒泡到外层 try/catch** → `await terrainPromise` 处炸死整个 boot IIFE,`#heroStill.is-shown` 永不撤岗=预热静态图永久盖住 canvas。现常量置于调用点上方(shader 编译段),且 executor 内自带 try/catch → `finish(null)` 走同步回退。**规则:IIFE 顶层 const 凡被启动路径引用,必须声明在首次调用执行点之前。****冷启动防卡死(2026-08-08,修"打开卡死 ~10s")**:三处防线,勿回退——① detect-gpu 已本地化到 `source/hero/vendor/`(detect-gpu.js + benchmarks/*.json,走 `benchmarksURL` 参数),勿改回 esm.sh/unpkg 运行时拉取;② 三个 shader 程序(main/cloud/aurora)先全部 `compileShader`+`linkProgram` 提交,再用 `KHR_parallel_shader_compile` 轮询 `COMPLETION_STATUS_KHR` 异步等待(无此扩展则状态查询自然同步阻塞=旧行为),uniform 读取必须在等待之后;③ 地形纹理生成在 Web Worker 里跑(`buildTerrainTextureAsync`,源码由函数 toString 拼接保证不漂移),与 shader 编译并行,失败回退同步 `buildTerrainTexture(512)`。所有程序的 `a_pos` 统一 `bindAttribLocation(p, 0, …)`(原来 sky pass 复用主程序 location 的写法已并入);④ 预热兜底(2026-08-08,修"编译期间黑屏"):live 确认后立即给 `#heroStill`(日出静态图,`fetchpriority="high"`)加 `is-shown` 盖住未绘制的 canvas,loop 内 `bootCur < 0.4`(场景从黑淡起过半)时移除交叉淡出,reduce 路径在 `draw(8.0)` 后直接撤——`freezeStaticHero`/`revealStaticFallback` 复用同一 `is-shown` 机制,不冲突。**v13.1(2026-08-28)动效收尾**:scrollY>60 → `body.hero-leaving`,typed 文案/`.hero-text`/底部居中滚动提示光线渐隐离场,回顶(≤60)恢复;hero→released 的 nav/#web_bg/body 背景/挂件(opacity)切换带 0.4-0.5s 过渡——这些都是场景上方的遮罩层,不碰 shader 输出,尾部溶解硬约束不受影响;提示光线(纯 CSS 光带循环)在 ≤768px 与 prefers-reduced-motion 下直接隐藏(窄屏 typed 长句会伸到屏幕中线,光线会被误读成打字光标)。**v14(2026-08-28)指针视差**:指针归一化坐标(-1..1)每帧缓动写 `u_par`,`renderValley` 里 `sx/sy += u_par` 只摇 3D 峡谷相机(上限 ~2.6°),天空渐变/太阳/光晕是 uv 空间层不随动,远墙 haze 又朝同像素 skyHaze 溶解,形成"近景移、远景定"的无缝层次视差;触屏/reduce 关闭,指针离窗或 scrollY>0.5vh 淡出。**别再碰 `u_mouse`/`u_mAmt` 复活旧河弯**——那段在 `field()` 里,而 field() 整条路径已在 main() 的 `#if 0` 之下(renderValley 射线峡谷取代了 ASCII 点阵地),写了也是死代码。**headless 目检教训**:无头 Edge(SwiftShader)编译巨 shader 要 ~35s,预热 still 撤岗(=`is-shown` 移除)才代表 loop 真起来了;此前的"动画没生效/截图静止"多半是没等够,不是 bug——`_shot_ui4.py` 的等待循环照此办理
  - `footer-river-inject.js`:footer 水流动画注入(canvas 在 `source/js/axtrivc-river.js`)
  - `cache-bust.js` / `inject-site-config.js`:资源版本号 / 站点配置注入
  - `post-desc-fix.js`:首页摘要修复(剔除正文内联 `<style>/<script>` 块,防止足球日报卡片预览漏 CSS)
- `scripts-py/football_daily_v2.py`:足球日报生成器 v6(GitHub Actions `football-daily.yml` 每天早/晚各跑一次,**FotMob 浅色卡片风休赛期模式**,世界杯模式已随赛事结束移除),写 `source/_posts/football-{morning|evening}-YYYY-MM-DD.md` 并**重建 `source/football/index.html` 归档页**(`update_archive_index()`,v1→v2 迁移时曾漏移植导致归档停更);`--archive-only` 只重建归档不拉数据,`--preview` 只写本地备份;本地备份目录默认 `D:/WorkBuddy-Outputs/Football-Daily`,可用 `BACKUP_DIR` 环境变量覆盖,目录不可建(CI/无 D 盘)时静默跳过
  - **联赛白名单(v6)**: 只展示 `MAIN_COMPS` 内的赛事——英超/西甲/意甲/德甲/法甲/沙特联/中超/美职联 + 欧冠/欧联 + 世界杯/欧洲杯/欧国联/国际友谊赛;巴甲/阿甲/英冠等小众联赛一律剔除,**但关注球队参加的任何赛事(如国王杯)始终保留**(`is_main_match()`);`COMP_MAP` 兼有"仅显示名"条目(国王杯/足总杯等,不在白名单);分组按中文名合并别名(uefa.champions ≡ champions-league),ESPN 直采 slug(esp.1/eng.1/ger.1/fra.1/ita.1/usa.1/fifa.friendly)已并入 `COMP_MAP`/`MAIN_COMPS`,组序先经 `CID_CANON` 归一再查 `COMP_ORDER`
  - 数据源: sports_skills CLI(daily 赛程) + **ESPN site API 直采**(球队赛程 `site.api.espn.com/.../soccer/{league}/teams/{tid}/schedule?season=`,按 FOLLOWED_TEAMS 的 espn 组合(联赛+欧冠+国内杯赛: 国王杯/足总杯/德国杯/法国杯/意大利杯)合并——sports_skills 的 get_team_schedule 数据陈旧已弃用,巴萨返回 0 场/西班牙只有 2024 欧洲杯;沙特联/中超不在 daily 覆盖,直采 `{ksa.1,chn.1}/scoreboard?dates=` 补齐) + Sky Sports 足球 RSS(`skysports.com/rss/11095`,feedparser 直解析拿 enclosure 配图,注意 12040 是综合体育频道别用错) + BBC/ESPN RSS(文字) + Google News(转会/球队动态);ESPN 事件两种结构(scoreboard 顶层 status / schedule 在 competitions[0].status,score 可能是 {$ref,value} 对象)由 `normalize_espn_event()` 统一
  - 图片: 队徽 `a.espncdn.com/i/teamlogos/soccer/500/{teamId}.png`(浏览器 UA 可直链,curl 不带 UA 会 404),联赛徽 `leaguelogos/soccer/500/{lid}.png`(英超 23/西甲 15/意甲 12/德甲 10/法甲 9/美职联 19/欧冠 2/欧联 2310/世界杯 4/欧洲杯 74/欧国联 2395/沙特联 2488/中超 2350),新闻图走 Sky 的 365dm CDN
  - 关键防线: **未赛 = 状态非 closed 且开球时间 > 当前时间**——数据源会把已踢完的比赛(如世界杯决赛)永久遗留为 not_started,只按状态过滤会把旧比赛当"下一场"展示;赛程按北京时间重新分桶(daily 接口按自身时区给日期会串天,沙特联常见 UTC 18:00 = 北京次日 02:00,extra 抓取要前后各多拉一天);daily 接口缺 MLS 等赛事,要用关注球队赛程合并补齐
  - 主题共存防线: Butterfly 的 `.container img` 会给文章内所有 img 注入 `display:block;margin:0 auto 20px`(特异性与 `.fm-*` 打平,靠文档顺序压制),**`.fm-container img{margin:0}` 不可删**,否则联赛徽标居中/关注卡队徽错位/缩略图偏上;fm 图片必须 `loading="eager"`——主题 post_lazyload 会把 src 改写成 1x1 占位 + `data-lazy-src` 由 vanilla-lazyload 滚动回载,原生 lazy 会让浏览器延迟发请求,快滚时被其 `cancel_on_exit` 取消导致大图永久空白;联赛 id 以数据源实际为准(阿甲 = `liga-argentina`,写错 id 会让联赛名显示英文原文)
  - 关注球队 9 支(v6,卡片可点击跳详情页): 巴萨 83 / 西班牙 164 / 曼城 382 / 曼联 360 / 阿森纳 359 / 拜仁 132 / 巴黎 160 / 科莫 2572 / 迈阿密国际 20232(旧档里的 81/78/1593/788 全部失效,勿复用;皇马 86/马竞 1068 已于 v6 移出关注)
  - 球队详情页: `source/football/teams/{barcelona,spain,mancity,manutd,arsenal,bayern,psg,como,miami}.html`——薄壳 + `window.TeamPage.init()` 配置,渲染全交给 `source/js/team-page.js`(下一场/最近5场/积分榜/数据总览,走 `site.api.espn.com` schedule + `site.web.api.espn.com` standings,客户端实时拉取)与 `team-dynamic.js`(新闻/阵容);**页面内不写死任何比分/排名**(v6 前的巴萨页硬编码欧冠晋级表已清除);国家队(西班牙)无积分榜,荣誉殿堂只放历史事实;日报关注卡、归档页 chips 均链接到这些页
  - 截图目检: `scripts-py/_shot_preview.py`(隔离预览);`scripts-py/_shot_public.py`(带主题真机渲染,需先 `node serve.js` 起 4000 端口,滚动需≥1s/屏模拟真人节奏,否则 lazyload 的 cancel_on_exit 会让 365dm 大图保持空白——截图假影非 bug)
- `scripts-py/github_daily.py`:GitHub 日报生成器(GitHub Actions `github-daily.yml` 每天 UTC 16:00 = 北京 0:00 跑一次,总结刚过去的北京日期),用 GitHub Search Commits API(`author:Axtrivc + committer-date` 区间,代码内按北京日期精确过滤)汇总当天所有仓库提交,模板化成中文文章写 `source/_posts/github-daily-YYYY-MM-DD.md`(分类 `GitHub 日报`);当天 0 提交则跳过不生成;`GH_TOKEN` 环境变量可选——CI 默认 `GITHUB_TOKEN` 只能搜公开仓库,**要纳入私有仓库需配 PAT secret `GH_ACTIVITY_TOKEN`(repo scope)**;支持手动传日期补生成:`python scripts-py/github_daily.py YYYY-MM-DD`(或 workflow_dispatch 的 `date` 输入)
- `scripts-py/gen_favicon.py`:站点 favicon 生成器(深夜色圆角方块 #101E38→#0B1220 + 等宽字母 A + #07C160 光标下划线,呼应 hero 夜色与 typed 终端美学),8x 超采样输出 `source/img/favicon.png`(64x64),由 `_config.butterfly.yml` 的 `favicon:` 引用;改设计后重跑 `python scripts-py/gen_favicon.py` 即可
- `source/js/theme-system.js`:5 主题切换系统,`themechange` 事件 + `--footer-*` / `--theme-*-current` CSS 变量
- `source/css/ui-polish.css` + `source/js/ui-polish.js`(经 `_config.butterfly.yml` inject 注入):全站版面/动效收尾层(进度线、日期防断行、卡片实体化、棕色清扫、入场编排等)。**入场动画(ui-reveal)只许用 `translate`/`opacity`,不许用 `transform`**——theme-system.css 的 `transform:none !important` 网格锁和第一轮的 hover 抬升都占用 transform;IO 交错入场用 `--reveal-delay` 同批 70ms 错峰、在"进入视口那一刻"分配(预分配会让深处卡片滚到时吃旧延迟),动画结束即摘除过渡类把 hover 还给卡片规则;`/page/2+` 的 full_page 头图已压到 220px(选择器 `body:not(.hero-page-active) #page-header.full_page`,首页本体由 hero-inject 置 height:0,两条各管各的);侧栏头像靠 `.loaded` 类淡入,ui-polish.js 有 complete/load 双兜底防止永久隐身。**第四轮(2026-08-28)动效**:跨页 View Transitions(板块 L,`@view-transition{navigation:auto}` 整页 0.32s 交叉淡化 + 卡片标题↔文章标题共享元素飞入,viewTransitionName 按文章路径生成——只命名 `#recent-posts` 主列表,侧栏最新文章同名会让整个过渡被跳过;中文 slug 剥 `%` 保合法 ident;Firefox/Safari 自动降级普通跳转)、文章页头视差(#post-info 随滚动 translate3d+渐隐,220px 页头内,纯 JS rAF 节流)、目录百分比徽标脉冲(板块 M,MutationObserver 监听数值变化,0.9s 节流重启 tocPulse 动画)。**第五轮(2026-08-28)**:① TOC 激活项主题化(板块 N)——Butterfly 默认 `#00c4b6` 青绿块是又一漏网硬编码(与棕色 #8b6f47 同源),现改主题色 12% 浅底+左侧指示条;② 入场编排推广到归档时间轴 `.article-sort-item`、标签页 `.tag-cloud-list`(注意主内容区是 tag-cloud-list,`.tag-cloud` 是侧栏卡片,别选错)、分类页 `.category-lists`、页脚 `.footer-other`;③ 主题切换过渡(板块 P)——`themechange` 时挂 `html.theme-gliding` 550ms,只列 body/nav/footer/卡片等大表面,不用 `*` 通配防重绘风暴;④ 文章图片 lazyload 淡入(板块 Q)——`#article-container img[data-lazy-src]` 占位期 `ui-img-wait`(opacity:0),load 后换 `ui-img-done` 播一次淡入,**已缓存图(complete 且 naturalWidth>1)不挂类防二次闪烁,load 回调里 naturalWidth≤1 还是 1x1 占位不算数**;⑤ 代码块/表格内部滚动条收细上主题色(板块 R,根滚动条已隐藏,内部滚动器保留)
- footer 配色衔接规则: body 背景 = `--page-bg`(页面色,内容列与两侧边距同色), html 背景 = `--footer-river-bottom`(overscroll 兜底,与水色最深处同色), **#footer 整体是一块连续渐变** `--page-bg 0% → --footer-river-top 55% → --footer-river-mid 82% → --footer-river-bottom 100%`(仿 river.ai 一体色块), river-stage 背景透明融在其中, canvas 顶部 38% 渐隐让波纹浮现 — 不要给 river-stage 单独设背景,否则会出现分界线
- 首页文章列表: `_config.butterfly.yml` 的 `index_layout: 6`(masonry) 会被 `theme-system.css` 末尾的 grid 规则覆盖为对齐双列(压掉 MasonryInfiniteGrid 的内联定位), 改布局时两边都要看
- 改 footer 配色后可用 `public/river-test.html?theme=<id>` 本地截图预览(源文件在 `source/river-test.html`,已加入 `skip_render`)
