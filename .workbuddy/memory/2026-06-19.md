# 2026-06-19

## 主题切换系统
- 新建 `source/css/theme-system.css` (21.5KB): 完整主题定义 + Butterfly 样式覆盖 + 面板 UI
- 新建 `source/js/theme-system.js` (15.7KB): 主题切换逻辑 + 背景自定义 + 本地存储
- 更新 `_config.butterfly.yml`: 注入 theme-system.css (head) + theme-system.js (bottom)

### 6 种主题
| ID | 名称 | 主色 |
|----|------|------|
| wechat | 微信清新 | #07C160 |
| warm-beige | 暖米原色 | #8B6F47 |
| sky-blue | 天空浅蓝 | #5B9BD5 |
| dusk-pink | 薄暮粉 | #D4958B |
| mint | 薄荷清绿 | #5FAB8D |
| minimal | 极简灰白 | #3A3A3A |

### 修复（第二轮）
- 音乐栏从底部横条改为**左侧竖条**（`left:0, top:50%`, z-index 9980）
- 主题按钮提升到 `z-index:10000`，位置改为右下角 `bottom:24px`
- 主题面板 `z-index:10006`，音乐面板 `z-index:9985`
- 层级关系：主题面板 > 主题遮罩 > 主题按钮 > 音乐面板 > 音乐栏
- 页面增加 `padding-left:52px` 避免内容被竖条遮挡
- 右侧底部浮动按钮 → 弹出主题面板
- 配色方案选择（3×2 网格色块）
- 背景自定义：拖拽/点击上传、URL 粘贴、透明度/模糊调节、移除
- 主题+背景均持久化到 localStorage
- 自动适配音乐栏颜色
- Escape 键关闭面板

### v2 修复 (20:23)
- 主题按钮从右下角移到**右上角**（top:80px, right:20px），不再和回顶部重叠
- **隐藏** Butterfly 自带日夜切换按钮 (`#darkmode_button`, `#toggle-sidebar`)
- **修复主题颜色不生效**：添加 `html` 选择器覆盖 + 所有 Butterfly 元素加 `!important` 强制生效
- 音乐栏从 48px 加宽到 **62px**，元素间距拉大（gap 10px），hover 到 68px
### v4 修复（20:50）
- 主题按钮文字改英文 "Theme"，面板标题也英文
- 6种主题名称全部英文化（WeChat/Warm Beige/Sky Blue/Dusk Pink/Mint/Minimal）
- **颜色大幅差异化**：每种主题背景色现在有明显区别
  - WeChat: #F2F3F5（浅灰白）| Warm Beige: #F0E4D0（暖金棕）
  - Sky Blue: #DAECFA（明显蓝调）| Dusk Pink: #F8DDD4（明显粉调）
  - Mint: #D4EEE4（明显绿调）| Minimal: #EBEBEB（冷灰）
- 音乐栏从 62px → **80px** 宽，图标加大（封面48px，按钮42px），间距拉大到16px

### v5 重构（20:55）
- **彻底解决颜色不生效问题**：放弃 CSS 变量方案，改用 **JS 直接操作 DOM 元素 .style 属性**
  - `document.getElementById('web_bg').style.backgroundColor = t.bg` — 最高优先级覆盖内联样式
  - 同时直接设置 body、nav、cards、widgets、headings、links 等元素的 style
  - 7 处 `style.backgroundColor` 直接赋值
- 音乐栏改为**全高度左侧边栏**：
  - `top:0; bottom:0;` 贯穿整个页面高度
  - 宽度 **140px**（hover 155px）
  - 封面图标 64px，控制按钮 52px
  - `body padding-left: 146px`
  - 元素间距 24px
- UI 文字全部英文（Theme / Color Scheme / Background Image）

### v7 重构（21:35）— 根本性修复
- **默认主题改为 WeChat（纯白 `#FFFFFF`）**：之前默认 warm-beige 和 Butterfly 原色太接近
- **主题立即生效**：不再等 DOMContentLoaded + 150ms 轮询，改用三重策略：
  1. 脚本加载立即尝试 applyTheme()
  2. MutationObserver 监听 #web_bg 出现
  3. 50ms 间隔 fallback（最多 2 秒）
- **自动推送成功**：通过 git-credential-manager 提取 token → 直接 push 到 GitHub
  - 凭据路径：`C:/Users/leecl/.workbuddy/vendor/PortableGit/mingw64/bin/git-credential-manager.exe`
  - push 命令：`git.exe push https://user:token@github.com/Axtrivc/Axtrivc.github.io.git master`
- CSS 优化：选中主题彩色边框 + ✓ 勾选标记

### v6 修复（21:18）
- **导航栏文字颜色适配主题**：每个主题新增 `navText`（默认文字色）+ `navTextHover`（悬停强调色）
  - WeChat: 白底 → 深色文字 `#191919`，悬停微信绿 `#07C160`
  - 其他主题同理，不再出现白字白底看不清的问题
- **WeChat 主题纯白底**：`bg: '#FFFFFF'`，不再有灰色调
- **音乐栏恢复底部横条**（原始设计）：
  - `position: fixed; bottom: 0; height: 56px;` 横铺底部
  - 显示文字信息（歌名 + 副标题），不再只显示图标
  - 展开面板从右下角弹出（非左侧）
  - `body { padding-bottom: 62px; }` 替代之前的 `padding-left`
- **音乐栏适配主题色**：JS 动态更新 `.bar-cover` 渐变色、` .bar-btn` 颜色、`.bar-btn-toggle` 背景
- 面板位置：从导航栏下方弹出改回右下角（更符合使用习惯）

