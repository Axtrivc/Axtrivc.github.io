# 2026-06-23

## Hero 动画：Glitch 故障风转场 v3

把之前平淡的"卷起 + clip-path"转场替换为 **Glitch 故障风**，效果震撼。

### 实现要点
- **8 条 `.hero-glitch-bar`** 错峰向上飞出（横向偏移 `(i-3.5) * 14px`、纵向 `-95vh - i*9vh`、旋转 `±8deg`）
- **SVG filter `#hero-rgb-split`** 用 feColorMatrix 把红绿蓝三通道分离，红左移 4px、蓝右移 4px、绿原位
- **`.hero-noise-spray`** 全屏噪点纹理，0.4s steps(8) animation 模拟扫描
- **文字破碎**：hero-text-headline skewY(-10deg) + translateY(-32vh)，副标题反向 skew
- **CTA 反向缩放飞出**

### 关键 CSS 技巧
- `mix-blend-mode: screen` 让 glitch bar 和 canvas 颜色叠加发光（无需复制 canvas 视觉）
- `feBlend mode="screen"` 在 SVG filter 内合成三通道
- 用 `--trigger = max(0, calc(var(--p) - 0.12))` 让所有 Glitch bar 在 0.12 才开始动，避免 0% 时就出戏
- `hue-rotate(calc(var(--i) * 22deg))` 给每条 bar 不同的色调，更"故障"

### 关键文件
- `scripts/hero-inject.js`：完整转场 CSS + SVG filter + glitch layer HTML
- `source/hero/river-hero.js?v=5`：含 visibilitychange 处理（之前已修复）

### 测试结果（hexo server localhost:4321）
- 0%：hero 完整可见，文字带 RGB 色差（霓虹灯感）
- 25%：glitch bar 飞出，文字模糊偏移
- 35-45%：撕裂感最强，标题飞散
- 50%：博客内容浮现
- 75%：完全过渡
- 100%：纯净博客主页
- 截图存 `D:/WorkBuddy-Outputs/_archive/glitch-{00,25,35,45,75,100}.png`

### 部署
- **2026-06-23 18:42 用户确认部署 v3（Glitch）** → `f4741796`
- **2026-06-23 19:00 用户报告 v3 掉帧，改回 v4（极简）** → `a69037b6`
- v4 极简转场：仅 opacity + translateY，零 filter/zero SVG filter/zero glitch bar
- 部署 commit: `a69037b6 fix(hero): 极简转场 v4`
- push: `f4741796..a69037b6 master -> master`
- GitHub Actions run #94 (Deploy Blog) In progress

### v3 → v4 关键变化（性能诊断）
- v3 掉帧根因：8 个 glitch bar will-change + SVG filter + mix-blend-mode + hue-rotate
  → Chromium 每帧栅格化 canvas 输出 + 多次 composite layer
- v4 极简：仅 `transform: translateY` + `opacity`（GPU 合成器原生路径）
- 删除：.hero-glitch-bar × 8、.hero-noise-spray、::before/::after、url(#hero-rgb-split)
- 视觉：river.ai 画面"顺流而上"飘远，干净过渡
- 进度导轨颜色：从白蓝（hero 内）改为深蓝（博客主题），避免视觉冲突

### 部署注意事项（已验证）
- public/ 在 .gitignore 里 → CI 自己跑 `npx hexo generate` 重建，无需手动 add public/
- 之前本地 `hexo generate` 的修改只影响本地测试，git add 全部会被 .gitignore 自动过滤
- 只需要 commit 源文件：scripts/hero-inject.js 等

---

## Skill 安装：superpowers（WorkBuddy Edition）

用户要求安装 **obra/superpowers**（GitHub 上 Claude Code 的 skills 框架）。

### 关键发现：obra/superpowers **不直接兼容 WorkBuddy**
- SKILL.md 用大量 `references/claude-code-tools.md`、`codex-tools.md`、`gemini-tools.md` 等平台特定映射
- `using-superpowers/SKILL.md` 要求"任何回复前必须 invoke skill"，依赖 Claude Code 的 Skill 工具运行时
- 有 `hooks/` 目录（Claude Code hooks 系统），WorkBuddy 不识别
- **结论**：直接装会让模型按 Claude Code 工具名调用 → 全错

### 安装决策：1+2 结合
- SkillHub 上 `wlshlad85/superpowers`（slug=`superpowers`）是已适配 OpenClaw 平台的工作流打包版
- 但仍有 OpenClaw 平台术语（`sessions_spawn` 等）需要改成 WorkBuddy 工具名
- 路径：`~/.workbuddy/skills/superpowers/`

### 已做的 WorkBuddy 适配
| 原（OpenClaw） | 改后（WorkBuddy） |
|---|---|
| `exec` 工具 | `Bash` tool |
| `sessions_spawn` | `Agent` tool with `subagent_type="general-purpose"` |
| 子 agent prompt 用 sessions_spawn | 改为 Agent tool + 结构化 self-contained prompt |
| 标题 | `Superpowers — OpenClaw Edition` → `Superpowers — WorkBuddy Edition` |
| description | "Requires exec tool and sessions_spawn" → "Requires Bash tool and Agent tool" |

### 安全审计结果
- ✅ 无 .sh/.py/.js/.ps1/.bat 可执行脚本
- ✅ 无二进制文件
- ✅ 无危险命令（rm -rf /, curl|sh, eval, reverse shell）
- ✅ 引用都自包含，无外部网络请求
- ⚠️ P1：原版有平台工具不匹配（已修复）
- ℹ️ P2：原 description 强制要求"先 invoke skill 才能回复"语义（保留，仅影响使用风格）

### 14 处 sessions_spawn 全部替换为 Agent tool（已验证 0 残留）

### 用法
- 触发词："let's build", "help me plan", "I want to add X", "this is broken"
- 主对话会强制进入 5 阶段：Brainstorm → Plan → Subagent-Driven Build → Code Review → Finish Branch
- 不要用：one-liner fixes、读代码、非代码任务
- river-hero.js ?v=5 cache-buster 已生效 → 用户刷新能看到最新版本

---

## 2026-06-23 19:55 用户偏好：默认使用 superpowers

**测试结果**：✅ Skill 工具正常加载 superpowers

**用户偏好**：以后所有 coding/feature/build 任务**默认走 superpowers 流程**
- "可以的话以后就给他设置为默认使用，记住我的选择"

**实践原则**（根据本项目经验）：
- 5 阶段流程太重时**可简化**：
  - 小改动（CSS 调整、bug 修复）：跳过完整 subagent 循环，直接 brainstorm → 改代码 → 自测
  - 中等改动（新功能、重构）：完整 brainstorm → plan → subagent
  - 大改动（架构、新 app）：严格 5 阶段
- Brainstorm 阶段问 1-2 个关键问题即可，不要问 10 个
- 用户已经明确需求的（"先阻力后下滑"）→ 直接进入 plan 阶段，不要反复确认

**Hero 转场重构**（即将开始）：
- 用户明确要求"先有阻力再下滑"
- 当前 v4/v5（极简）→ 重构
- 需要先 brainstorm：阻力曲线形状（指数？弹性？黏性？）+ 触发阈值

## v7「弹簧蓄力→爆裂释放」重构（21:10 commit 3a4fe2b6）

用户第三次要求重构转场，明确"先有阻力再下滑"。
用户同时要求清理首页：浮动播放器、右下角按钮、blog-info、site-info、scroll-down 全删，只留 hero + 顶部 nav。

### 阻力→释放数学模型
- 临界点 raw = 0.45（45% 滚动量时 hero 蓄满势能）
- **蓄力段**（raw ∈ [0, 0.45]）：
  - 视觉进度用 `easeInQuad(pin) × 0.22`（前慢后快蓄能）
  - shell `translateY 0 → +7vh`（被往下"压"）
  - shell `scale 1.0 → 1.08`（鼓胀感）
  - 文字 `translateY 0 → -2vh`（轻微被弹簧拉起）
  - 进度导轨慢填充（脉动圆点存在）
- **释放段**（raw ∈ [0.45, 1.0]）：
  - 视觉进度用 `0.22 + easeOutExpo × 0.78`（前 15% 缓、后 85% 极速）
  - shell `translateY +7vh → -200vh`（飞出屏幕）
  - shell `scale 1.08 → 1.0`
  - 文字散开 ±25vh + 飞走 -60vh（headline 左 / cta 右 / quote 中）
  - main 从 +100vh 滑入 + 淡入
  - 浮点播放器等被 `display:none`

### CSS 关键设计
- `--hero-pin` / `--hero-easeout` 两个独立变量驱动两段
- shell `transform: translate3d(0, pin*7vh - ease*207vh, 0)`
- section.scale `1.0 + pin*0.08 - ease*0.08`
- 文字 alternate direction（headline 左、cta 右）增强爆裂感
- 博客 main z-index 1100 在 hero-shell 1000 之上，避免闪烁

### 性能铁律（v3→v4→v5→v6→v7 经验）
- ❌ 不在 hero-shell 上用 CSS filter / SVG filter / mix-blend-mode / will-change
- ✅ 只用 transform: translate3d + scale + opacity
- ✅ canvas 单独加 translateZ(0)
- ✅ 0 transition → 滚动 100% 跟手

### 部署
- commit `3a4fe2b6 fix(hero): v7 「弹簧蓄力→爆裂释放」重构转场`
- push `8e47a712..3a4fe2b6`
- hexo deploy `b2b6393 → main`
- 验证：grep 出 easeInQuad×7、easeOutExpo×7、弹簧蓄力×1

### ⚠️ Agent 卡死根因（与本任务无关）
- agent-browser 调用 stuck 时表象是 agent "莫名自停"
- 真正原因：Chrome DevTools 端口（默认 9223）被残留进程/TCP TIME_WAIT 占满
- 预防：每次用前先 `Get-NetTCPConnection -LocalPort 9223 | Stop-Process -Id {$_.OwningProcess} -Force` 清理
- 或指定递增端口：`--remote-debugging-port=9224` 等
- 已写入 ~/.workbuddy/MEMORY.md（"调试陷阱"段）

---

## v9「滚动阻力 → 临界爆发 → 急速飞走」重构（2026-06-23 21:46 commit 20d4b0af）

用户第 4 次要求重构转场，要求"先有阻力再直接往下滑"，并要求清理首屏净化（去掉 nav/bday/音乐/fab/publish 浮窗）。

### 三段式数学模型
- **阻力段** [raw 0, 0.30]：visual 0 → 0.10（`easeInQuart` t^4）
  - 用户"用力滚动"但 hero 几乎不动
  - 内部 `::after` vignette 逐渐加深（蓄水感）
  - 文字保持原位
- **临界过渡** [raw 0.30, 0.50]：visual 0.10 → 0.22（线性）
  - 阻力消失，蓄能溢出
  - nav 顶部开始 translateY(-100% → 0) 滑入
- **爆发释放** [raw 0.50, 1.0]：visual 0.22 → 1.0（`easeOutExpo`）
  - hero 急速向上飞出 -200vh
  - main 从 +88vh 滑入 + 淡入

### 净化规则覆盖（必须包含）
```
#music-bar, #music-panel, #rightside, #go-up, #rightside-config, #aside,
.bday-countdown, #publish-button, .fab-publish, [class*="publish"],
#card-sponsors, .announcement, .card-announcement,
#axtrivc-fab, .hexo-fab, #live2d, #live2d-widget, [class*="live2d"]
```

### Nav 同步下滑实现
```css
--purge-progress: clamp((var(--hero-visual, 0) - 0.10) / 0.55, 0, 1);
opacity: var(--purge-progress);
transform: translate3d(0, calc((1 - var(--purge-progress)) * -100%), 0);
```
visual=0.10 起步，visual=0.65 完全显示，nav 跟着 main 同步进场。

### 蓄力段压力感 vignette
```css
.hero-shell::after {
  background: radial-gradient(ellipse at center, transparent 30%, rgba(0,0,0,0.45) 95%);
  opacity: var(--hero-pin, 0);
}
```
纯 background 而非 box-shadow / filter → 性能安全（v3-v4 教训）。

### 部署
- commit `20d4b0af fix(hero): v9 「滚动阻力 → 临界爆发 → 急速飞走」+ nav 同步下滑 + 净化浮窗`
- push `88bc11bd..20d4b0af master -> master`
- GitHub Actions Deploy Blog ✅ success
- 生产验证：axtrivc-fab / easeInQuart / bday-countdown / live2d-widget / purge-progress / 压力感 全部命中

### 调试陷阱（已确认）
- hexo server 改 scripts/hero-inject.js 后**不会自动 reload CSS**
- 必须 `taskkill /F /PID <pid>` + 重启 server
- 判断：curl 输出 grep 期望字符串若 0 → server 还是老版

---

## v10「nav 始终显示 + logo 白色 + flex 布局修正」（commit 9786be2b，run #99）

用户反馈："其他做的非常好，就是顶部的导航栏我没看见啊"
问题根因：v9 把整个 `#page-header.full_page` 隐藏了，**导致 nav 也没了**。

### 三层修复
1. **nav 永远显示**：`position: fixed; top:0; left:0; right:0; height:60px`
2. **flex 布局修正**：`flex-direction: row; justify-content: space-between`
   - `#blog-info`（logo）在左
   - `#menus`（菜单项）在右（**必须设 `flex: 0 0 auto` + `width: auto`** 否则会撑满覆盖 logo）
3. **logo 文字强制白色**：`#blog-info .site-name { color: #fff !important }`
   - 原 inline `style="color: rgb(10, 61, 42)"`（深绿）跟金棕背景几乎同色

### 净化规则微调
- 删除 `#blog-info { display: none }`（v9 错误）
- 保留 `#site-info`、`#scroll-down` 隐藏（首屏 hero 已替代）
- `#menus` 不再跟随 `--purge-progress`

### 滚动 → main 阶段后
- `body.hero-past #nav { background: #b8860b !important }`（从渐变金棕 → 纯色）

### 部署
- commit `9786be2b fix(hero): v10 nav 始终显示 + logo 白色 + flex 布局修正`
- push `20d4b0af..9786be2b`
- GitHub Actions Deploy Blog run #99 in progress

### 调试教训（下次别踩）
- `display: flex` + 没设 `flex: 0 0 auto` + 没设 `width: auto` → 子元素会撑满父级覆盖其他兄弟
- 检查 nav 布局问题用 `getBoundingClientRect` 看每个子元素的实际 rect（不是只看 display）
- `style="color: rgb(10, 61, 42)"` 这种 inline style 比 CSS 选择器优先级高 → 必须 `!important` 才能覆盖
- agent-browser 截图中左下角"+"号不是网站元素，是工具栏浮窗（用户报告时不要被误导）
